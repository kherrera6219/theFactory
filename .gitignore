# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
.pytest_cache/
.pytest-tmp*/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
.next/
dist/
*.tsbuildinfo

# Packaged installers (large binaries; build via `npm run electron:package`)
*.exe
apps/mission-control/playwright-report/
apps/mission-control/test-results/

# Env and local
.env
.env.*
!.env.example
*.local

# OS/IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/worktrees/

# Logs and runtime
*.log
*.pid

# Build artifacts
build/
out/

# Runtime outputs
backups/
MagicMock/
output/

# Generated test/lint/coverage artifacts (produced by CI and local runs)
coverage.xml
ruff_errors.txt
lint_errors.txt
.secrets.baseline
reports/
artifacts/

# Secrets / credentials (populate then encrypt before committing)
config/agent_api_keys.yaml
config/agent_api_keys.enc.yaml
*.age
*.sops.yaml
deploy/.local/

# Live API keys for local testing — never commit
test-keys/

# Shared sandbox workspace for RQCA / behavioural-equivalence execution.
# Contents are throwaway per-mission directories.
.sandbox-workspace/
